{{- if .Values.backup.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "mcp-guardian.fullname" . }}-backup
  labels:
    {{- include "mcp-guardian.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.backup.schedule | quote }}
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: backup
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              command: ["/bin/sh", "-c"]
              args:
                - cp {{ .Values.persistence.mountPath }}/history.db /backup/history-$(date +%Y%m%d).db
              volumeMounts:
                - name: data
                  mountPath: {{ .Values.persistence.mountPath }}
                  readOnly: true
                - name: backup
                  mountPath: /backup
          volumes:
            - name: data
              persistentVolumeClaim:
                claimName: {{ include "mcp-guardian.fullname" . }}-data
            - name: backup
              persistentVolumeClaim:
                claimName: {{ include "mcp-guardian.fullname" . }}-backup
{{- end }}
